#pragma once

#include "types.h"

namespace Cmn {
    namespace Def
    {
        enum class WeaponKind {
            cNone = -1,
            cMain = 0,
            cSub = 1,
            cSpecial = 2
        };
    };
};