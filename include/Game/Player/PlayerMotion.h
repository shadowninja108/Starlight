#pragma once

#include "types.h"

namespace Game {
    class PlayerMotion {
        public:
        enum AnimID {
            
        };
        void startEventAnim(Game::PlayerMotion::AnimID, float, float);
    };
};